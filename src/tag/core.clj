(ns tag.core
  (:require [tag.fetch :refer [rhyming-words]]
            [tag.db :as db])
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))








;; (spur)
